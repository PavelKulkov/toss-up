package controllers

import (
	"net/http"
)

func GenerateTimetable(w http.ResponseWriter, r* http.Request) {
	//for e := range groups {
	//	table := services.GenerateTimeTable(groups[e])
	//	services.SaveTimetables(table)
	//}
}
